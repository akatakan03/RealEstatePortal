@using RealEstatePortal.Domain.Enums
@model RealEstatePortal.Application.Listings.Commands.UpdateListing.UpdateListingCommand
@{
    ViewData["Title"] = "Edit Listing";
    var photos = ViewBag.Photos as List<RealEstatePortal.Application.Listings.Queries.GetListingImages.ListingImageDto>
                 ?? new List<RealEstatePortal.Application.Listings.Queries.GetListingImages.ListingImageDto>();
}

<div class="form-page">
    <div class="container">
        <div class="form-shell">
            <span class="eyebrow">Agent dashboard</span>
            <h1 class="page-title" style="margin-bottom:1.5rem;">Edit listing</h1>

            <div class="form-card">
                <form asp-action="Edit" method="post" enctype="multipart/form-data">
                    <input type="hidden" asp-for="Id" />
                    <div asp-validation-summary="ModelOnly" class="text-danger" style="margin-bottom:1rem;"></div>

                    <div class="mb-field">
                        <label asp-for="Title" class="form-label"></label>
                        <input asp-for="Title" class="form-control" />
                        <span asp-validation-for="Title" class="text-danger"></span>
                    </div>
                    <div class="mb-field">
                        <label asp-for="Description" class="form-label"></label>
                        <textarea asp-for="Description" class="form-control" rows="4"></textarea>
                        <span asp-validation-for="Description" class="text-danger"></span>
                    </div>
                    <div class="form-row">
                        <div class="mb-field">
                            <label asp-for="ListingType" class="form-label"></label>
                            <select asp-for="ListingType" asp-items="Html.GetEnumSelectList<ListingType>()" class="form-select"></select>
                        </div>
                        <div class="mb-field">
                            <label asp-for="PropertyType" class="form-label"></label>
                            <select asp-for="PropertyType" asp-items="Html.GetEnumSelectList<PropertyType>()" class="form-select"></select>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="mb-field">
                            <label asp-for="Price" class="form-label"></label>
                            <input asp-for="Price" class="form-control" />
                            <span asp-validation-for="Price" class="text-danger"></span>
                        </div>
                        <div class="mb-field">
                            <label asp-for="Currency" class="form-label"></label>
                            <input asp-for="Currency" class="form-control" />
                            <span asp-validation-for="Currency" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-row-3">
                        <div class="mb-field">
                            <label asp-for="Bedrooms" class="form-label"></label>
                            <input asp-for="Bedrooms" class="form-control" />
                            <span asp-validation-for="Bedrooms" class="text-danger"></span>
                        </div>
                        <div class="mb-field">
                            <label asp-for="Bathrooms" class="form-label"></label>
                            <input asp-for="Bathrooms" class="form-control" />
                            <span asp-validation-for="Bathrooms" class="text-danger"></span>
                        </div>
                        <div class="mb-field">
                            <label asp-for="AreaSqMeters" class="form-label"></label>
                            <input asp-for="AreaSqMeters" class="form-control" />
                            <span asp-validation-for="AreaSqMeters" class="text-danger"></span>
                        </div>
                    </div>

                    <h3 style="font-family:var(--serif);font-weight:600;font-size:1.2rem;margin:1.8rem 0 1rem;">Features &amp; details</h3>

                    <div class="form-row">
                        <div class="mb-field">
                            <label asp-for="Heating" class="form-label">Heating</label>
                            <select asp-for="Heating" asp-items="Html.GetEnumSelectList<RealEstatePortal.Domain.Enums.HeatingType>()" class="form-select">
                                <option value="">Not specified</option>
                            </select>
                        </div>
                        <div class="mb-field">
                            <label asp-for="Internet" class="form-label">Internet</label>
                            <select asp-for="Internet" asp-items="Html.GetEnumSelectList<RealEstatePortal.Domain.Enums.InternetInfrastructure>()" class="form-select">
                                <option value="">Not specified</option>
                            </select>
                        </div>
                    </div>

                    <div class="form-row-3">
                        <div class="mb-field">
                            <label asp-for="FloorNumber" class="form-label">Floor</label>
                            <input asp-for="FloorNumber" class="form-control" />
                        </div>
                        <div class="mb-field">
                            <label asp-for="TotalFloors" class="form-label">Total floors</label>
                            <input asp-for="TotalFloors" class="form-control" />
                        </div>
                        <div class="mb-field">
                            <label asp-for="BuildingAge" class="form-label">Building age (yrs)</label>
                            <input asp-for="BuildingAge" class="form-control" />
                        </div>
                    </div>

                    <div class="mb-field">
                        <label asp-for="MonthlyDues" class="form-label">Monthly dues (aidat)</label>
                        <input asp-for="MonthlyDues" class="form-control" />
                    </div>

                    <div style="display:flex;gap:1.5rem;flex-wrap:wrap;margin-bottom:.5rem;">
                        <label style="display:flex;align-items:center;gap:.5rem;">
                            <input type="checkbox" asp-for="IsFurnished" /> Furnished
                        </label>
                        <label style="display:flex;align-items:center;gap:.5rem;">
                            <input type="checkbox" asp-for="HasBalcony" /> Balcony
                        </label>
                        <label style="display:flex;align-items:center;gap:.5rem;">
                            <input type="checkbox" asp-for="HasParking" /> Parking / garage
                        </label>
                    </div>

                    <div class="mb-field">
                        <label asp-for="Address" class="form-label"></label>
                        <div class="input-with-btn">
                            <input asp-for="Address" class="form-control" />
                            <button type="button" class="btn btn-ghost" id="locateBtn">Locate on map</button>
                        </div>
                        <p class="form-hint">Type the address and click "Locate on map", then drag the pin to fine-tune.</p>
                    </div>

                    <input type="hidden" asp-for="Latitude" />
                    <input type="hidden" asp-for="Longitude" />
                    <div id="pickerMap"></div>

                    <div class="mb-field" style="margin-top:1.4rem;">
                        <label class="form-label">Add photos</label>
                        <input type="file" name="newPhotos" multiple accept="image/*" class="form-control" id="newPhotos" />
                        <p class="form-hint">Uploaded when you click Save.</p>
                        <div id="previewArea" class="photo-grid"></div>
                    </div>

                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">Save changes</button>
                        <a asp-controller="Dashboard" asp-action="Index" class="btn btn-ghost">Cancel</a>
                    </div>
                </form>

                @* Photo management (separate forms — cannot nest inside the edit form) *@
                <div style="margin-top:2rem;padding-top:1.6rem;border-top:1px solid var(--line);">
                    <h3 style="font-family:var(--serif);font-weight:600;font-size:1.2rem;margin:0 0 1rem;">Current photos</h3>
                    @if (photos.Any())
                    {
                        <div class="photo-grid">
                            @foreach (var photo in photos)
                            {
                                <div class="photo-item">
                                    <img src="@photo.ThumbnailUrl" alt="listing photo" />
                                    <div class="actions">
                                        @if (photo.IsCover)
                                        {
                                            <span class="badge-status badge-sold">Cover</span>
                                        }
                                        else
                                        {
                                            <form asp-action="SetCover" method="post">
                                                <input type="hidden" name="listingId" value="@Model.Id" />
                                                <input type="hidden" name="imageId" value="@photo.Id" />
                                                <button type="submit" class="btn btn-ghost btn-sm">Set cover</button>
                                            </form>
                                        }
                                        <form asp-action="DeletePhoto" method="post" onsubmit="return confirm('Delete this photo? This cannot be undone.');">
                                            <input type="hidden" name="listingId" value="@Model.Id" />
                                            <input type="hidden" name="imageId" value="@photo.Id" />
                                            <button type="submit" class="btn btn-danger btn-sm">✕</button>
                                        </form>
                                    </div>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <p style="color:var(--muted);">No photos yet.</p>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Head {
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.getElementById('newPhotos').addEventListener('change', function (e) {
            var area = document.getElementById('previewArea');
            area.innerHTML = '';
            for (const file of e.target.files) {
                if (!file.type.startsWith('image/')) continue;
                var img = document.createElement('img');
                img.className = 'preview-thumb';
                img.src = URL.createObjectURL(file);
                area.appendChild(img);
            }
        });
    </script>
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="~/js/location-picker.js"></script>
    <script>
        @{
            var initLat = Model.Latitude?.ToString(System.Globalization.CultureInfo.InvariantCulture) ?? "null";
            var initLng = Model.Longitude?.ToString(System.Globalization.CultureInfo.InvariantCulture) ?? "null";
        }
        initLocationPicker({
        mapId:'pickerMap', addressId:'Address', latId:'Latitude', lngId:'Longitude',
        locateBtnId:'locateBtn', initialLat:@initLat, initialLng:@initLng
        });
    </script>
}